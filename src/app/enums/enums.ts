export enum Cellstate {
    untouched = 'untouched',
    revealed = 'revealed',
    flagged = 'flagged',
}

export enum Sound {
  revealCell = 'revealCell',
  flagCell = 'flagCell',
  win = 'win',
  lost = 'lost',
}